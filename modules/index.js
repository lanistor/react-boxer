import { addElement, removeElement, clearElement } from './BoxerManager'

export const Boxer = {
  add: addElement,
  remove: removeElement,
  clear: clearElement
}

export { default as Box } from './Box'
